<Window x:Class="Port_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Port_Viewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="400"
        Loaded="Window_Loaded">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Port" HorizontalAlignment="Center" FontSize="24" FontWeight="DemiBold"/>
            <Label Content="PID" Grid.Column="1" HorizontalAlignment="Center" FontSize="24" FontWeight="DemiBold"/>
        </Grid>
        <Separator DockPanel.Dock="Top" Margin="10,0,10,0"/>
        <ScrollViewer Margin="10,10,10,10" Background="LightGray">
            <Grid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>
    </DockPanel>
    
</Window>
