<Window x:Class="Port_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Port_Viewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="400"
        Loaded="Window_Loaded">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Port" HorizontalAlignment="Center" FontSize="24" FontWeight="DemiBold"/>
            <Label Content="PID" Grid.Column="1" HorizontalAlignment="Center" FontSize="24" FontWeight="DemiBold"/>
        </Grid>
        <Separator DockPanel.Dock="Top" Margin="10,0,10,0"/>
        <DockPanel DockPanel.Dock="Top" Margin="10,10,10,0">
            <Button Content="Refresh" Click="Refresh_Button_Click"/>
            <Menu>
                <MenuItem Header="Sort By">
                    <MenuItem x:Name="IPort" Header="Increasing Port" Click="Sort_By_Click"/>
                    <MenuItem x:Name="DPort" Header="Decreasing Port" Click="Sort_By_Click"/>
                    <MenuItem x:Name="IPID" Header="Increasing PID" Click="Sort_By_Click"/>
                    <MenuItem x:Name="DPID" Header="Decreasing PID" Click="Sort_By_Click"/>
                </MenuItem>
            </Menu>
            <Rectangle DockPanel.Dock="Left" MinWidth="110"/>
            <Menu DockPanel.Dock="Right">
                <MenuItem Header="Search For">
                    <MenuItem Header="Port" Click="Search_For_Click"/>
                    <MenuItem Header="PID" Click="Search_For_Click"/>
                </MenuItem>
            </Menu>
            <TextBox DockPanel.Dock="Right"/>
            
        </DockPanel>
        <ScrollViewer Margin="10" Background="LightGray">
            <Grid x:Name="grid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>
    </DockPanel>
    
</Window>
